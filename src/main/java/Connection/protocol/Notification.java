package Connection.protocol;

public class Notification {
    // more like notifications should hava interface with notifyUser(notification info)
    // doesn't quite make sense
    // something from server to client
}
